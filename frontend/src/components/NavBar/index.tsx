import React from 'react';

import { NavBarContainer } from './styles';

const NavBar: React.FC = () => {
  return (
    <NavBarContainer>
      <h1>Tá Na Mesa</h1>
    </NavBarContainer>
  );
};

export default NavBar;
